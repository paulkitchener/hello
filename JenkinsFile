pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
		git branch 'pk', url 'https://github.com/paulkitchener/hello.git'
		sh 'javac HelloWorld.java'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
		sh './run.sh'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
